language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t jamespmccullough/docker-react -f Dockerfile.dev .    

script:
    - docker run -e CI=true jamespmccullough/docker-react npm run test    